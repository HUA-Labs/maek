interface:
  display_name: "maek Loop"
  short_description: "Turn planning files into the next execution step."
  brand_color: "#0F766E"
  default_prompt: "Use $maek-loop to turn planning files into the next small verified implementation step."

policy:
  allow_implicit_invocation: true
