interface:
  display_name: "maek Bootstrap"
  short_description: "Start work in a repo with fast context setup."
  brand_color: "#0F766E"
  default_prompt: "Use $maek-bootstrap to establish the current repo context before making changes."

policy:
  allow_implicit_invocation: true
