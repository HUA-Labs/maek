interface:
  display_name: "maek Consumer QA"
  short_description: "Validate install, build, and runtime as a consumer."
  brand_color: "#0F766E"
  default_prompt: "Use $maek-consumer-qa to verify a fresh consumer app can install, build, and run."

policy:
  allow_implicit_invocation: true
